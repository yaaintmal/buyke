# Frontend Environment Variables Example

# App title and slogan (example values)
VITE_APP_TITLE=BlickBuy
VITE_APP_SLOGAN=shop it!

# Optional defaults for first-time setup (frontend-only):
# VITE_DEFAULT_LANGUAGE=en         # 'en' or 'de' (used when no saved language exists)
# VITE_DEFAULT_AVATAR=v1           # 'v1' | 'v2' | 'v3' (used when no avatar saved)
# VITE_DEFAULT_EXTENDED_FUNCTIONS=true # 'true' or 'false' (used when no saved preference exists)

# Frontend API base URL (used by the client). Set to the full origin of your outsourced backend.
# For local development, you can leave this unset and the app will use http://localhost:5000 as a fallback.
# VITE_API_URL=https://api.example.com
